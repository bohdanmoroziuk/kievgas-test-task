import { OrderUsersByPipe } from './order-users-by.pipe';

describe('OrderUsersByPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderUsersByPipe();
    expect(pipe).toBeTruthy();
  });
});
